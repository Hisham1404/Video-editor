# Install torch FIRST, matched to the instance's CUDA version, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# Installing it via this file can pull a CPU-only or mismatched build.

transformers>=4.57.0
accelerate>=1.0.0
huggingface_hub>=0.26.0
pillow>=10.0.0
opencv-python-headless>=4.10.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# Gemini arm only; skip if running local models exclusively.
google-genai>=1.0.0
